/** Shared breakpoints for phone / tablet / desktop layouts. */
export const BREAKPOINTS = {
  tablet: 768,
  desktop: 1024,
} as const;

/** Max content width for list/form screens on wide viewports. */
export const CONTENT_MAX_WIDTH = 720;

export const CONTENT_WIDE_MAX_WIDTH = 960;

/** Fixed width for the map detail side panel on tablet and web. */
export const MAP_SIDE_PANEL_WIDTH = 400;

export const MAP_SIDE_PANEL_WIDTH_DESKTOP = 440;

/** Narrow sidebar width when tabs move to the left edge on wide web. */
export const SIDE_TAB_BAR_WIDTH = 88;

/** Centered modal max width on tablet and web. */
export const MODAL_MAX_WIDTH = 560;

export const MODAL_WIDE_MAX_WIDTH = 640;
